services:
  docs:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5174:5174"
    environment:
      NODE_ENV: development
    command: sh -c "npm ci && npm run docs:dev -- --host 0.0.0.0 --port 5174 --strictPort"
